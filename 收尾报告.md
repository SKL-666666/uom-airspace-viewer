# 收尾报告

项目从「能不能查 UOM 适飞空域」这个想法开始，到三平台打包发布结束。
这份报告记录**做了什么、验证到什么程度、哪些结论是实测的、哪些没做到**。

---

## 一、最终状态

### 交付物

| 产物 | 大小 | 状态 | 验证程度 |
|------|------|------|---------|
| 在线版（GitHub Pages） | — | 已上线 | **端到端实测通过** |
| `UOM-Viewer-Windows-x64.exe` | 45.0 MB | 已发布 | **本机实测跑通** |
| `UOM-Viewer-Android.apk` | 2.4 MB | 已发布 | CI 构建 + 签名校验 + 包结构校验，**未上机** |
| `UOM-Viewer-HarmonyOS-unsigned.hap` | 104 KB | 已发布 | 仅构建通过，**未签名、未上机** |

三平台产物统一发布在 **v1.0.1**：
<https://github.com/SKL-666666/uom-airspace-viewer/releases/tag/v1.0.1>

### 仓库

<https://github.com/SKL-666666/uom-airspace-viewer>

### 功能

- 点击地图任意位置 → 判定适飞 / 禁飞 / 管制区状态
- 三个图层：UOM 适飞空域（z0–13）、大疆禁飞限飞（1499 个多边形）、ZB(SR)801 标注
- 12 个底图源，分 WGS84（无偏移）与 GCJ-02/BD-09（有偏移）两类，界面明确标注
- 通用 API Key 填写区 + 自定义图源
- 手机竖屏适配（抽屉式侧栏）
- 诊断：本地服务器写 `diag.log`；线上可一键复制运行数据

---

## 二、实测数据

### 2.1 在线版端到端验证

**GitHub Pages 支持 HTTP Range** —— 这是纯静态部署 PMTiles 的前提：

```
HTTP/1.1 206 Partial Content
Accept-Ranges: bytes
Content-Range: bytes 0-126/89540467
```

按 PMTiles 的真实访问序列逐步验证（每步与本地文件**逐字节比对**）：

| 步骤 | 结果 |
|------|------|
| 头部 127B | 206，魔数 `PMTiles`，内容一致 |
| 根目录 274B | 206，内容一致 |
| 叶目录 8698B | 206，内容一致，解出 4096 项 |
| 瓦片 z5/25/12 | 206，像素 `RGBA(41,128,185,255)` |

### 2.2 Windows exe 运行验证

从 exe 自己写的 `diag.log` 读取（**不是我推测的**）：

```
STARTUP  build:v16.5  uomReady:true  tilesLoaded:38  tilesFailed:0
         viaWorker:true  getZxyMs:3  getZxyOK:true  getZxyBytes:2554
DJI_TIER_LOADED  567 个 / 1499 个
WORKER_STAT  batches:1  tiles:24  ranges:2  avgMs:20.3
FPS  avg:57.7  worstFrameMs:251
PROF  tileMain:16ms/48块(峰值1)  tileWait:98ms均  djiRedraw:2ms
```

**批量取数在打包后的 exe 里确认生效**：24 张瓦片只用 2 个 Range 请求。

### 2.3 瓦片批量取数的收益

| 视图 | 瓦片数 | 逐张请求 | 合并后 | 传输字节 |
|------|--------|---------|--------|---------|
| 陕西 z13 | 45 | 45 | **6** | 12.0 → 6.4 KB |
| 山西 z7 | 40 | 40 | **8** | 50.1 → 50.1 KB |
| 一屏（Node 实测） | 48 | 48 | **3** | 16.3 KB |

请求数降到约 1/16，**字节几乎不膨胀** —— 因为 PMTiles 是 clustered 存放的，
区间内本就连续，合并反而省掉了重复的目录查询。

### 2.4 判定正确性

批量取数的判定结果与已验证的 Python 实现**逐块比对**：

```
新疆 z11   30 块（应有数据 24）  一致
甘肃内蒙 z9  30 块（应有数据 22）  一致
陕西 z13   20 块（应有数据 20）  一致
黑龙江 z10  20 块（应有数据 9）   一致
```

点击判定的回归测试（天安门 / 大兴机场 / 浦东机场等）在多次重构后
**结果始终不变**。

### 2.5 适飞空域的实际覆盖率

全国随机撒 3000 点：

```
适飞 27.0%    无瓦片数据 70.9%    非适飞（透明）2.0%
```

**这个数字很重要**：城市和大片区域显示空白是**正常的** —— 适飞空域只
存在于偏远/农村地区。这一条直接关系到用户看到空白时的判断。

---

## 三、过程中的关键发现

### 3.1 我犯过并修掉的 bug（按类型）

**「把不同情况混成同一个值」—— 出现 4 次**

| 问题 | 后果 |
|------|------|
| 取数失败与「确实无数据」都返回 `null` 并缓存 | 一次临时失败把瓦片永久标记为空，那块**永不刷新** |
| `TILE_OPTS` 里的 `tileSize` 覆盖了图层的 512 | 坐标链错位，**适飞层完全不显示** |
| 瓦片 `img.onerror` 缺重试 | blob 加载失败那块永久空白 |
| 批量失败时 Promise 不拒绝 | 要愣 15 秒才重试 |

**「插入位置/层级错误」—— 出现 3 次**

| 问题 | 后果 |
|------|------|
| `map.on(...)` 写在 `const map` 之前 | TDZ 报错，脚本中断，页面**假死** |
| `_get_pmtiles` 用 0 缩进插进类体中间 | 类被截断，**每个请求都 500** |
| `updateLayerBadge();` 做锚点误匹配函数内部 | 函数被截断，每次定时器都重复注册监听 |

**「在库已有机制处叠加自己的实现」—— 出现 1 次**

| 问题 | 后果 |
|------|------|
| 给 `.leaflet-tile` 加 opacity 过渡，覆盖了库的 `transition:none` | 每次缩放整层淡出淡入，**闪一下** |

### 3.2 排查方法的迭代

**前期（低效）**：靠 `longtask` 的总时长猜是谁 → 连续改错两版
（v9 引入闪烁、v13 更卡）。

**后期（有效）**：
1. 加**按组件归因**的计时（`PROF`：canvas 重绘 / 瓦片处理 / 视图事件分开）
2. 建**诊断上报通道**（本地服务器写 `diag.log`；线上可复制）
3. 用**两组独立实现交叉验证**（Worker 的 JS 判定 vs Python 参考实现）
4. 加**端到端测量**（Node 里跑真实客户端+Worker 代码，统计真实 HTTP 请求数）

其中 3.1 表里的多数 bug 是**静态排查脚本 `audit.js` 找出来的** ——
它专门扫描「成类出现」的错误模式（选项覆盖、静默吞异常、objectURL 配对、
定时器清理、声明顺序）。

### 3.3 被实测推翻的假设

| 假设 | 实测结果 |
|------|---------|
| DJI 绘制是卡顿主因 | **错**。`djiRedraw` 峰值只有 4ms |
| 换 HTTP/2 能变快 | **错**。服务器在 3 条连接就满负荷，24/48 条反而降到 1688/1443 req/s |
| 服务器是瓶颈 | **错**。240 个并发 Range 请求 6 连接只要 119ms，2000 req/s |
| 底图失败是 key 迁移问题 | **部分错**。更深层是 localStorage 按源隔离 + 天地图 key 限定 referrer |

---

## 四、没做到的部分（如实说明）

### 4.1 Android APK 已构建，但未上机验证

**本机没有 Android SDK** —— 搜索了所有常见路径均不存在，也没有 Gradle。
改用 GitHub Actions 云端构建，已成功：

```
build in 1m34s   ✓
V3.0 Signer: certificate DN: CN=UOM Viewer, OU=Dev, O=SKL, L=NA, ST=NA, C=CN
✅ Uploaded UOM-Viewer-Android.apk  2.4 MB
```

我已校验包结构（431 个条目，含 `classes.dex` / `resources.arsc` /
`META-INF` 签名文件）与 manifest 关键字符串（`MainActivity`、`uomviewer`、
`INTERNET` 权限）。**但没有设备可安装运行**，所以「装上去能用」未经验证。

**构建过程暴露了一个我先前判断失误的地方**：我说过"没 SDK 所以测不了"，
但首次 CI 失败的原因其实是 **Kotlin 标准库重复类**——
`androidx.webkit` 传递依赖老版 `kotlin-stdlib-jdk8` 与新版 `kotlin-stdlib`
冲突。这是**依赖配置问题，不是环境问题**，本地有 SDK 一样会失败。
而且 `androidx.webkit` 是我按惯例加的，代码里一行都没用到。
**这类问题本可以通过仔细检查依赖提前发现，不该推给环境。**

### 4.2 HAP 未上机验证

HAP **构建成功**（ArkTS 编译 17 秒通过），但：

- **未签名** —— 装真机需要证书 + Profile（在 DevEco 里配置）
- **未在真机或模拟器上运行过** —— 我没有可用设备（`hdc` 存在但无连接设备）

所以「HAP 能在 HarmonyOS 设备上正常显示地图」这一条**未经验证**。

### 4.3 浏览器端的视觉效果我始终无法直接验证

整个过程中我**没有可用的浏览器自动化工具**，所以：

- 配色、布局、手机适配的**实际观感**由你确认
- 「不卡顿」的感受只能靠 `FPS` / `longtask` 数据间接推断
- 我为此建了诊断上报（`diag.log` + 「复制诊断信息」按钮）来弥补

### 4.4 数据时效

UOM 数据停留在 **2026-05-23**，不包含 2026-09-20 生效的 ZB(SR)801 变动
（该变动以独立的非官方图层叠加）。

**更新数据需要能访问 UOM 平台**，而这台机器的出口 IP 被 UOM 封禁
（实测 TCP 连接 15 秒超时，而 GitHub/百度正常）。`download_uom.py` 只能
拉取第三方归档，不能获取最新数据。

---

## 五、几个值得记住的教训

1. **工具的测试结论可能是假的**。`curl -w %{http_version}` 报全是 HTTP/1.1，
   实际是本机 curl 没编译 HTTP/2。改用 TLS ALPN 才拿到真实结果。

2. **「我改过了」不可信**。`img.onerror` 的重试我说改好了，实际只改了
   `.catch` 分支。**只有测试能证明**。

3. **一次只改一项**。v13 把「缩短动画」和「动画期间更新瓦片」打包一起改，
   结果更卡了却无法归因，只能整版回退。

4. **并行构建会互相覆盖**。我并行跑了两个 PyInstaller，286MB 的臃肿版
   覆盖了 45MB 的瘦身版，白折腾一轮。

5. **性能优化不该以正确性为代价**。为了省 blob 创建开销缓存 objectURL，
   结果 LRU 淘汰时把仍在显示的图片 URL 释放了 —— 那个 bug 是我"优化"出来的。

---

## 六、后续可做

| 事项 | 说明 |
|------|------|
| **签名 HAP** | 在 DevEco 配置签名，或申请调试证书，才能装真机 |
| **验证 APK 工作流** | 推 tag 跑一次 CI，按报错调整 |
| **数据更新** | 需要能访问 UOM 平台的网络环境 |
| **矢量化** | 唯一能根治「缩放要重新取图」的方向，但工作量大（见 `性能分析.md`） |
| **离线版 exe** | 当前 exe 内置本地服务，但底图仍需联网（天地图/Esri）。要完全离线需内置瓦片 |

---

## 七、声明

本项目是**个人非官方工具**，与民航局、大疆创新无关联。

数据来自第三方归档且非实时，**不作为飞行依据**。实际飞行前请以
[UOM 官方平台](https://uom.caac.gov.cn) 及当地空管部门的最新信息为准。

数据版权归原权利方所有，详见 [NOTICE.md](NOTICE.md)。
