# 收工文档

> 本文档记录 UOM 适飞空域查询工具从零到当前状态的全部工作，
> 包括做了什么、怎么做的、踩过哪些坑、哪些**没有验证**，以及后续方向。
>
> 最后更新：2026-09-22　构建版本：**v19.2**　Release：**v2.0.0**
>
> 写作原则：**只写有依据的内容**。凡是"实测得到"都注明怎么测的；
> 凡是"未验证"就明确写未验证，不含糊过去。

---

## 一、这个项目是什么

### 它解决什么问题

飞无人机前需要确认一块地方能不能飞。官方渠道是 UOM 平台
（`uom.caac.gov.cn`），但需要登录、交互也不方便随时查。
这个工具把数据取到本地，点一下就出结果。

### 三项判定

| 判定项 | 数据来源 | 方法 |
|--------|---------|------|
| **是否在适飞空域内** | UOM 适飞空域栅格（第三方归档） | 读像素 alpha（z13），精确到约 19 米 |
| **是否命中大疆禁飞/限飞区** | DJI FlySafe 公开数据（1499 条） | 点在多边形内判定 |
| **是否在首都禁飞区范围** | 用户自行标注的 300km 圆 | 点在多边形内判定 |

结论优先级**不可调换**：`ZB(SR)801 管制区 > 大疆禁飞区 > UOM 适飞 > 非适飞 > 无法判定`。
这条优先级有独立测试盯着（`test_query_logic.js`），因为它是安全相关的，
而且极容易在重构中被"顺手调换"——界面上只会显示一个不一样的结论，不会报错。

### 定性（重要）

**非官方、不作为飞行依据。** 数据来自第三方归档、非实时。
工具内所有页面都写明了这一点，`NOTICE.md` 里也标注了数据归属
（CAAC 拥有 UOM 数据，DJI 拥有 flysafe 数据）。

---

## 二、当前交付物

### 产物

| 平台 | 文件 | 大小 | 数据 | 签名 |
|------|------|------|------|------|
| **Android** | `UOM-Viewer-Android.apk` | **90.5 MB** | 全部内置 | CI 一次性调试密钥 |
| **HarmonyOS** | `UOM-Viewer-HarmonyOS-unsigned.hap` | **88.4 MB** | 全部内置 | **未签名** |
| **网页版** | GitHub Pages | - | 远程读取 | - |

产物都在 [Releases](https://github.com/SKL-666666/uom-airspace-viewer/releases)。

体积几乎全是数据：UOM 栅格 85.4 MB（本身是压缩瓦片，无法再压）。

### 代码结构

```
UOM/
├── index.html                 查看器（单文件，5113 行，含全部前端逻辑）
├── tile-worker.js             Web Worker：批量取数 + PMTiles 目录解析
├── serve.py                   本地 HTTP 服务（自实现 Range 支持）
│
├── data/
│   ├── uom-shifei.pmtiles     UOM 适飞空域栅格，z0–13，85.4 MB
│   ├── dji_cn_full.geojson    大疆禁飞/限飞（中国境内，1499 条，1.9 MB）
│   ├── dji_cn_low.geojson     同上，低缩放概览版（567 条）
│   ├── dji_flysafe.geojson    大疆全球原始数据（4112 条，5.4 MB，不进安装包）
│   └── custom_zones.geojson   ZB(SR)801 标注（用户自定义，非官方）
│
├── android/                   Android 工程（Java + WebView + 本地 Range 服务）
├── harmony/                   HarmonyOS 工程（ArkTS + ArkWeb + 本地 Range 服务）
├── .github/workflows/         CI：云端构建 APK
│
├── 测试与工具（38 个文件，详见第八节）
└── 文档（README / 收工文档 / 性能分析 / NOTICE）
```

---

## 三、关键技术决策（附依据）

### 1. PMTiles + HTTP Range：一切的地基

UOM 适飞空域是**栅格数据**，被做成 PMTiles 单文件（85.4 MB）。
PMTiles 的读取方式是：

```
① 读文件头 127 字节            → 得到根目录位置与元信息
② 读根目录（约 274 字节）      → 得到叶子目录位置
③ 按需跳读叶子目录 + 瓦片数据  → 每次都指定字节区间
```

**全程依赖 HTTP Range（206）**。这不是可选优化，是硬约束：

- `file://` 协议**不发 Range 请求**
- Android 的 `assets://`、HarmonyOS 的 `rawfile://` **只能整体读取**
- 直接双击 `index.html` 打不开

所以：**任何环境都必须有一个支持 Range 的 HTTP 服务在前**。
这一条决定了后面很多设计。

### 2. 为什么移动端要"应用内起服务"

早期 APK/HAP 是**加载 GitHub Pages 的外壳**。问题在于实测：

| 项 | 实测值 |
|---|---|
| GitHub Pages DNS 解析 | 0.02 s |
| **GitHub Pages TCP 建连** | **10~15 s**（10 次全部如此） |
| 天地图瓦片域 TCP 建连 | 0.017 s |

**卡在 TCP 握手，不在 DNS。** 而无人机使用场景常在野外没有稳定网络，
15 秒建连基本等于不可用。

改成：**数据打进安装包 + App 启动时在 `127.0.0.1` 起一个只读、支持 Range
的本地服务**，页面走 `http://127.0.0.1:<port>/index.html`。

这个设计与本地开发用 `python serve.py` **完全同构**，所以
**`index.html` 一行都不用为移动端改**（数据路径本来就是相对的）。

### 3. 坐标系换算：修掉一个 300~600 米的隐性错误

**数据**（UOM 栅格、大疆、ZB(SR)801）是 **WGS84/CGCS2000**；
**底图**里高德/腾讯是 **GCJ-02**、百度是 **BD-09**。同一地点相差 **300~600 米**。

早期版本**拿点击坐标直接查数据**，于是：在高德底图上点 A 点，
判定的其实是偏 500 米的 B 点。**界面上完全看不出来**（标记还在鼠标位置），
但边界附近足以得出相反结论。

修法：**判定一律用 WGS84，只在"把标记画回底图"这一步换到底图坐标系**。
实测北京偏 555 m，修完往返误差 0.000 cm。

**改不掉的残留问题**：适飞层是 WGS84 栅格，铺在 GCJ-02 底图上，
**瓦片网格本身错位**，所以蓝色层相对高德地图上的道路仍会偏。
这个在界面上明确写了"不要拿它核对边界"。

### 4. 批量取数：把 48 个请求压到 3 个

一屏有几十块瓦片，逐张取就是几十个 HTTP 请求，而浏览器对同域名只开 6 条连接。

两级优化：
1. **客户端攒批**：同一轮内的请求合成一个消息发给 Worker
2. **Worker 合并字节区间**：按 PMTiles 文件顺序，把相邻（间隔 ≤256B）的瓦片
   合并成一个 Range 请求

实测：**48 块瓦片 → 3 个请求**（16 倍）。

### 5. 分级加载大疆数据

全国概览时，94% 的多边形在屏幕上不到 3 像素，肉眼看不见，
但 Leaflet 的 canvas 每次重绘仍要为每个多边形各付一次 `fill()+stroke()`。
4112 个要素 = 8224 次调用，实测每次重绘约 **160 ms** —— 这就是卡顿来源。

做法：低缩放只用 883 个"屏幕尺寸 ≥3px"的要素（开销降到 1/4.6）。
**注意：分级只影响"画什么"，不影响"判定什么"**——点击判定必须用全量数据，
否则低缩放下会漏判小区域。空域漏判是安全问题，不能为省性能牺牲正确性。

---

## 四、界面重构（v17 → v19）

用户反馈"UI 太土"，做了一轮完整重构。布局改为
**顶部搜索栏 + 右侧竖工具条 + 底部抽屉**，设置项收进浮层面板。

### 解决的具体问题

| 用户报的问题 | 根因 | 做法 |
|---|---|---|
| **字体无法跟随系统** | 字号全部写死 px | 改 `system-ui` + rem；**并修掉 `html{font-size:.8125rem}` 这个把 1rem 钉在 13px 的错误**（导致所有组件比设计值小 19%，且"跟随系统"形同虚设） |
| **二级下拉字体太大** | **原生 `<select>` 的展开列表由操作系统绘制，CSS 完全管不到** | 自写下拉控件替代 |
| **放大缩小按钮和数据位置冲突** | 左下缩放控件被底部状态条压住 | 状态条整个删掉，状态移进设置面板；底部只留右下角一个坐标读数 |
| **测距看不出数字** | 数字放在右上角的 Leaflet 控件里，**正好被搜索栏盖住** | 改为顶部居中独立面板，数字用大字 |
| **测距不该双击结束** | 双击会连发两次 click，等于多点一个顶点 | 改为"先选测距/测面积 → 画 → 点确定" |
| **图层/调节按钮功能重复** | 两个按钮打开同一个面板 | 只留一个 |
| **去 AI 味** | 毛玻璃 + 大圆角 + 多层柔光投影 + 胶囊圆角 + emoji | 全部收掉；圆角 18px→10px，投影改单层，emoji 换纯文字 |
| **深色背景偏蓝** | 深色色值带蓝调（`#191b1f`/`#24272c`） | 改中性灰阶（`#0a0a0a`~`#2e2e2e`），实测全部令牌 RGB 三通道色差 = 0 |
| **点搜索结果要平滑变焦** | `setView({animate})` 是线性插值，跨省像瞬移 | 改 `flyTo`（弧线 + 缓动），落地后再出查询结果 |
| **结果要标注省市** | 只显示了 address | 用高德的 `pname/cityname/adname`，显示「省 · 市 · 区」并做去重（直辖市 省=市） |

### 顺带加的护栏（这是本轮最有价值的产出）

重构中我写出的 bug，静态检查抓不到，最后靠真实浏览器才发现。
于是补了三个静态检查 + 一个浏览器自检：

| 工具 | 作用 |
|---|---|
| `check_ids.js` | 脚本引用的 id 必须都在标记里；id 不能重复；**脚本切换的 class 必须有能命中该元素的样式规则** |
| `check_refs.js` | 调用的函数必须有出处；**零参数调用声明了形参的函数**直接报错 |
| `check_order.js` | 顶层声明顺序（防 TDZ），含"符号表里的名字都必须存在"的自检 |
| `make_probe.py` + `cdp_ui_test.js` | **用无头 Chrome 在真实时间里驱动页面并断言** |

**浏览器自检当场抓出 3 个静态检查漏掉的真 bug：**
1. `provinceEntries()` 里用了另一个作用域的循环变量 → `ReferenceError` → **搜索整个不可用**
2. 面板"再点一次收起"的条件里多加了个 `&& !secId` → **图层按钮永远关不掉**
3. 测量清除条件写错 → **测完清不掉**

**结论：静态检查只能证明"名字对得上、语法能过"，证明不了"点下去有反应"。**

---

## 五、地名搜索：这条线走了十几轮，值得单独记录

### 最终结论

**搜索已可用，走的是高德 Web 服务通道。天地图搜索通道已移除。**

### 走过的五个坑（每一个都有实测依据）

| # | 坑 | 现象 | 依据 |
|---|---|---|---|
| 1 | **缺 `level`/`mapBound`** | 严重 | 天地图返回 `{"cndesc":"缺少参数：level","infocode":2003}`；实测该接口必须限定搜索空间 |
| 2 | **把成功当失败** ⭐主因 | `服务正常（1000）` 却搜不出来 | 成功时 `cndesc` 恰好是文案"服务正常"，而我写的是 `if (cndesc) throw` —— **任何成功返回都在读 POI 前被抛掉** |
| 3 | **坐标是 `lonlat` 合并字符串** | 有数据却解析出 0 条 | 实际返回 `{"lonlat":"116.4,39.9"}`，不是分开的 `lon`/`lat` |
| 4 | **`level` 与 `mapBound` 自相矛盾** | 时灵时不灵 | 实测到 `level 5` 配东亚整片视野，而北京并不在该范围内 |
| 5 | **关键字太宽泛时接口不返回列表** | 报"认不出结构" | 实测「机场」命中 2196 条，顶层只有 `count/statistics/prompt`，**没有 `pois`** |
| 6 | **中文输入法未处理** | 搜索框里搜不出来 | `grep isComposing` 零命中；用拼音打字时输入框里先是拼音，被拿去搜了 |
| 7 | **传了不存在的变量名** | 一直报"解析出错" | 我给解析函数传了 `rawText` 而真实变量叫 `text` → **每次调用都抛 `ReferenceError`** |

### 天地图搜索为什么最终放弃

**不是 key 问题，不是网络问题 —— 是它的前置 WAF 在拦。**

实测响应：

```
HTTP/1.1 418
Server: CW
Set-Cookie: HWWAFSESID=9cf514d8e9f23a7bfc6
Block-Event-Id: 90-10-140-20260921225817-8e2df074
```

- `Server: CW` + `HWWAFSESID` = 华为云 WAF 在回话
- **`Block-Event-Id` = 这次请求被主动拦截**（有编号就是"我拒绝了你"，不是"连不上"）
- `418` 是它惯用的拒绝码

**注意拦截发生在哪一层**：`api.tianditu.gov.cn` 的 **TCP 连接是通的**
（实测 28 ms 就能建连），被拦的是**应用层**——请求发出去了，WAF 拒绝处理并回 418。
所以现象不是"连不上"，而是"连上了但拿不到数据"，这更容易被误判成网络问题。

**关键区分**：天地图有**两个域名**，走**不同链路**：

| 域名 | 用途 | 实测 |
|---|---|---|
| `t0.tianditu.gov.cn` | **底图瓦片** | ✅ 通（27~74 ms），请求正常返回 |
| `api.tianditu.gov.cn` | **地名搜索** | TCP 通（28 ms），但**请求被 WAF 拦**（418 + Block-Event-Id） |

所以"底图正常、搜索不行"不矛盾。**底图保留，搜索通道移除。**

**我不会去绕这个 WAF** —— 那属于规避服务方的访问控制，而且这套 WAF 本来就是拦爬虫的。

### 为什么改用高德

| 对比项 | 天地图 | 高德 Web 服务 |
|---|---|---|
| CORS | `*`（通） | `*`（通） |
| 首都机场 | 命中 958 条但结构不稳 | **北京首都国际机场 + T3 + T2**（POI 本体） |
| 华强北 | 嫌关键词太宽泛，不给列表 | 3 条有效 POI |
| 前置 WAF | **有，会拦** | 无 |
| key 类型要求 | 瓦片与搜索可能是不同服务 | 必须申请**「Web服务」**类型 |

**实测结论：高德 POI 数据明显更全、更准**，尤其"机场/园区设施"这类
无人机场景最需要的目标。

### 也要记录的备选方案考察

| 服务 | 结论 |
|---|---|
| **Photon（OSM）** | CORS 通、免 key，但实测搜「首都机场」只返回「首都机场路」「首都机场路北口」——**没有机场本体**。搜设施类目标会给出**错误位置**（点在路上了），比搜不到更危险，故未采用 |
| **百度 Place API** | **无 CORS 响应头**，浏览器无法直连（只能 JSONP），未采用 |
| **Nominatim（OSM）** | 国内实测**连接超时 20s**，不可用 |

---

## 六、数据更新：现状与可行路径

### 现状（各数据的可更新性完全不同）

| 数据 | 来源 | 能否更新 |
|---|---|---|
| **大疆 FlySafe** | `www-api.dji.com/api/geo/areas`（大疆官方公开接口） | ✅ **可再生**：按机型参数扫描即得 |
| **UOM 适飞空域** | 第三方归档（参考 `thezzisu/uomtiles`） | ❌ **不可再生**，只能等归档更新 |
| 自定义管制区 | 用户自己标注 | 手动 |

大疆那份的溯源信息还留在数据里：

```
endpoint     = https://www-api.dji.com/api/geo/areas
drone        = dji-mavic-3
country      = CN
radius_m     = 50000        step_km = 70      query_points = 4631
generated_at = 2026-05-30T06:50:59.598Z
```

### UOM 有没有接口

**有内部接口，但没有对公众开放的官方 API，也没有申请渠道。**

网页前端在调后端接口，但这些接口需要登录态、通常带签名/referer 校验，
无文档、无配额说明。**"能调"不等于"允许调"**。

**合法拿到只有一条路：UOM 平台自己有没有导出/申请功能** —— 这个需要去平台上确认。

### 建议的更新方案（按投入排序）

1. **数据源做成可配置**（推荐先做）
   现在数据路径写死在 `index.html` 与 `tile-worker.js`，改成可配置后，
   换数据 = 换地址，不需要改代码。并加"数据截至 XXXX-XX-XX"显示，过期一眼可见。

2. **写转换脚本 `build_tiles.py`**
   把任何格式的新数据（GeoJSON/SHP/KML）转成 PMTiles。
   纯技术工作，不涉及任何访问控制。

3. **大疆单独做更新脚本**（手动运行，不自动）
   数据源是大疆官方公开接口，可重跑扫描。
   **建议做成"手动跑一次"，不做定时/常驻** —— 是否允许批量自动调用，
   取决于对方服务条款。

4. **UOM 部分**：先去平台确认有无导出功能，再决定。

### 一个必须说清的现实

**适飞空域每月可能变，而任何第三方归档都不该作为飞行依据。**

- **飞之前**：上 UOM 官方平台查那个具体位置，或用官方 App
- **平时**：用这个工具做**大致规划**（这片大概能不能飞、附近有没有大疆禁飞区）

**这不是推卸责任，而是数据新鲜度决定了它能承担的用途。**

---

## 七、已知限制与未验证项

### ❌ 未验证（必须明确）

| 项 | 说明 |
|---|---|
| **APK 真机运行** | 我手上没有 Android 设备。只做到"CI 构建成功 + 包内数据校验通过" |
| **HAP 真机运行** | 同样没有鸿蒙设备，只做到"本地构建成功 + 包内资源核对完整" |
| **HAP 安装** | **需用你自己的华为开发者账号签名**，我无法代签 |
| **真机上的定位** | 无头浏览器没有定位源，只验证了失败分支（错误分类与提示） |
| **剪贴板实际内容** | 只验证了"不抛错"，没验证真复制出了什么 |
| **文件导入** | 无头环境没有文件选择器，未验证真实文件导入 |
| **UI 观感** | **我在整个过程中看不到图片**，配色/间距/图标只能靠用户判断 |

**装了之后若地图空白**，请反馈。两端都有日志：
- Android：`adb logcat` 看 `LocalAssetServer` / `MainActivity` 标签
- HarmonyOS：DevEco 日志窗口看 `UOMViewer` 域

日志里能直接看到：服务是否启动成功、端口是多少、请求返回什么状态码。

### 已知限制

| 限制 | 原因 | 影响 |
|---|---|---|
| **适飞层放大后有方块锯齿** | 栅格数据原生到 z13，一个像素约 19 米；z14+ 是拉伸显示 | 边界呈阶梯状。**这是数据精度上限，不是渲染 bug** |
| **适飞空域只覆盖约三成国土** | 数据本身如此 | 大片空白是**没有数据**，既不是禁飞也不是适飞 |
| **放大时蓝色范围会略微收窄** | 低层级瓦片把边界"聚合上色"，层级越高越精确 | 观感上像"少了"，实际是变清晰 |
| **适飞层在 GCJ-02 底图上仍错位** | WGS84 栅格铺在 GCJ-02 底图上，瓦片网格本身错位 | 已自动换算判定坐标，但图层视觉错位无法消除 |
| **GitHub Pages 国内建连 10~15 秒** | 卡在 TCP 握手 | 移动端已改为数据内置绕开；网页版仍在 |
| **搜索依赖高德 key** | 天地图搜索被 WAF 拦 | key 存 localStorage；换设备需重填 |
| 栅格转矢量不可行 | 实测估算需约 103 MB、431 万顶点，且**判定精度会变差**（多边形近似） | 已评估并放弃，详见工作记录 |

---

## 八、测试与工具清单

### 一条命令跑全部

```bash
python serve.py 8080        # 先起服务（部分用例依赖 HTTP Range）
node run_all_tests.js       # 18 项全跑
node run_all_tests.js --fast  # 跳过耗时的批量/端到端
```

当前状态：**18 / 18 通过**。

### 测试清单

| 文件 | 覆盖 |
|---|---|
| `check_ids.js` | 标记与脚本的 id / class 接口一致性 |
| `check_refs.js` | 函数引用与调用参数个数 |
| `check_order.js` | 顶层声明顺序（防 TDZ） |
| `audit.js` | 静态排查（选项覆盖、空 catch、定时器、死代码） |
| `test_geoconv.js` | 坐标换算（偏移区间、正反互逆、境外不变、按底图分派、接线检查） |
| `test_query_logic.js` | 判定优先级、几何入参、边界退化 |
| `test_search.js` | 搜索参数（含 `level`/`mapBound` 自洽）、坐标解析、兜底识别 |
| `test_geocode_real.js` | **用真实返回形状跑真实解析代码** |
| `test_range_server.py` | Range 解析逻辑（22 项，与两端实现同一套用例） |
| `test_tile_cache.js` | 瓦片缓存语义（失败 vs 无数据、去重、并发合并） |
| `test_batch_client.js` | 客户端攒批 |
| `test_batch_worker.js` | 批量取数（含与本地文件字节比对） |
| `test_batch_truth.js` | 与 Python 参考实现交叉验证"无数据"判定 |
| `test_batch_e2e.js` | 端到端请求数测量 |
| `test_http_range.py` | 服务端 Range 实现正确性 |
| `verify_logic.py` | 判定逻辑的 Python 参考实现 |
| `cdp_ui_test.js` + `make_probe.py` | **真实浏览器驱动 + 断言** |

### 构建与校验工具

| 文件 | 作用 |
|---|---|
| `make_icons.py` | 生成图标（单色几何符号，各平台尺寸） |
| `sync_assets.py` | 同步网页资源与数据到 Android assets |
| `sync_rawfile.py` | 同步到 HarmonyOS rawfile |
| `verify_apk_data.py` | 校验 APK 内含数据且 pmtiles 未被压缩 |
| `pmtiles_tool.py` | Python 版 PMTiles 读取器（用于交叉验证） |
| `build_dji_cn.py` / `build_dji_tiers.py` | 从大疆原始数据生成中国境内与分级数据 |
| `build_custom_zones.py` | 生成自定义管制区 |

---

## 九、这一轮工作的经验教训

这部分是给未来的自己看的，写得直白些。

### 1. 测试写错比代码写错更危险

本轮**至少 6 次**出现"测试失败 → 我以为是代码 bug → 去改代码"，
结果发现是**测试本身写错**：

- 断言要求"缩放进度与位移进度同步"，但 `flyTo` 的特征就是先平移后缩放
- 单点采样受时序影响，120ms 有时已越过平移阶段 → **偶发误判**
- 正则里的反向引用 `\1` 被转义吃掉 → 那条断言**从未生效**
- 探针把点选验证放在搜索循环内，状态被后续搜索覆盖 → 误判"点了没反应"
- `vm` 沙箱里**顶层 `const` 不挂到沙箱对象上**，`sandbox.X` 恒为 `undefined`
- `parseCoord` 只提取了源码没求值，却当函数调用

**结论：测试也要求证。失败先问"是代码错还是断言错"。**

### 2. 把异常原文摊开，比继续加推测分支有效得多

搜索那条线走了十几轮。真正的转折点是**不再猜、直接把原始返回打出来**：

- 加上"原始返回结构转储"后，一次就看到了 `lonlat` 字段
- 加上"搜索框路径实测"后，一次就看到了 `rawText is not defined`

**之前几轮我一直在加猜测性的分支（猜字段名、猜参数），每一轮都要用户配合试一次。**

### 3. 先核对版本再排查

我加了 `BUILD` 版本号，却没在排查时**先核对版本**。
有一轮用户报的现象其实来自旧版页面，我却在追一个已经不存在的 bug。

### 4. 不要因为"看起来像"就下结论

- 用户说"包头一片放大后没了"，我查了 z8 和 z13 都有数据，就答"没丢图"。
  **实际是中间层级的问题，我查的两级恰好是"两头"**——是用户追问才逼出正确结论。
- 我说"该处本来就没数据"，当时**没有依据**。实测覆盖率 87% 之后才确认
  确实有局部成片空白。

### 5. 静态检查抓不到"变量名不存在"

`check_refs.js` 能抓"少传形参"，但抓不到"**传了一个作用域里不存在的变量名**"——
因为那是个裸标识符，不是属性访问。`rawText` 那个 bug 就是这么活了十几轮。

**这类问题只有真跑才看得见**，所以浏览器自检不可省。

### 6. 移动端的"完成"必须是真机上跑过

我早期把 APK/HAP 标成"完成"，实际**从未在设备上运行过**。
这一轮明确标注了未验证项 —— 这是应该保持的做法。

---

## 十、后续方向

### 优先级建议

**高：**
1. **真机验证 APK / HAP**（这是当前最大的未知）
2. **数据源做成可配置 + 数据日期显示**（让"更新数据"不再需要改代码）
3. **HAP 签名**（需用户账号）

**中：**
4. 大疆数据的**手动更新脚本**（数据源是官方公开接口，可重跑）
5. GitHub Pages 的**建连慢**问题：换托管（Cloudflare Pages 等）或做国内镜像
6. 界面上把"空白 = 无数据"用图例说清楚，减少误判为 bug

**低（可选功能）：**
7. 批量点位查询结果导出 CSV
8. 收藏分组与备注
9. 图例显示当前视野的数据覆盖率（一眼看出"这片是本来稀还是丢图"）

### 关于数据合规（必须坚持）

- **不做**：绕过 UOM 登录/风控抓取其数据
- **不做**：篡改官方栅格数据（哪怕只是本地自用）
- **坚持**：数据来源可追溯、过期可见、定性为非官方参考

用户最初问过"能不能拿到 UOM 最新数据"和"能不能手动改某个范围是否禁飞"，
这两条我都拒绝了，并给出了替代方案（更换数据源、独立的自定义图层）。
**这个立场不因后续需求变化而变。**

---

## 十一、快速上手

### 本地开发

```bash
python serve.py 8080
# 打开 http://127.0.0.1:8080/index.html
```

> **不能双击 `index.html`** —— 必须有 HTTP 服务提供 Range。

### 改完代码后

```bash
node run_all_tests.js          # 18 项检查
node make_probe.py             # 生成带探针的页面（浏览器自检用）
node cdp_ui_test.js            # 真实浏览器自检
```

### 构建移动端

```bash
# Android（本机无 SDK，走 CI）
gh workflow run build-android-apk

# HarmonyOS（本地可构建，产物未签名）
bash harmony/scripts/build_hap.sh
```

两个构建脚本都会**先自动同步资源**（`sync_assets.py` / `sync_rawfile.py`），
再构建。资源同步目录不入版本库。

### 换图标

```bash
python make_icons.py    # 改 make_icons.py 里的绘制逻辑即可
```

---

## 附：本轮提交记录（打包之后）

```
4e7f2fa feat(mobile): APK/HAP 改为数据全内置 + 应用内 Range 服务；换图标
ca4b3c0 feat(search): 结果标注省·市·区；深色主题改为中性纯黑；修飞行回调可能不触发
4fe810a feat(map): 搜索结果 / 定位 / 收藏 改为平滑飞行动画
f7bf961 fix(search): 天地图移出搜索链；tooBroad 不再短路整条通道链
bb8c319 fix(search): Failed to fetch 要分类并给出可操作提示
4c68eea fix(search): 关键词太宽泛时接口不返回列表，误报成"认不出结构"
e7b1db3 fix(search): 找到并修掉 —— 传给解析函数的是个不存在的变量名（rawText）
ccad3c7 fix(search): 认不出的返回结构自动记录；坐标提取加按内容的兜底
51340c6 fix(search): 根本原因 —— 代码完全没有处理中文输入法组字
5a3cf74 feat(search): 内置 57 个常用地点（离线可搜）
117f16d fix(search): 修两处会让"搜到东西却不显示"的缺陷；并修正我自己的误报
c7a9f18 diag(search): 把响应解析抽出为独立函数并加抛错保护
60f52ca fix(diag): 搜索成功时不再打印失败排查对照表
a8af928 fix(search): level 改为由 mapBound 跨度反推
c7ec77c docs(search): 记录调试这个接口踩过的四个坑
5618f9c fix(search): 成功判定写反了 —— "服务正常（1000）"被当成错误抛掉
b6831f7 fix(search): 支持 lonlat 合并字段
1a57365 fix(search): 天地图响应解析改为按形状识别
```

再往前是界面重构（v17–v19）与三平台打包（v1.0.0 / v1.0.1）两批提交。
