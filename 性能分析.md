# 性能分析：为什么高德网页版更流畅

本文记录实测数据与推断的区分。**标注〔实测〕的是本地跑出来的结果，标注〔推断〕的是基于公开信息的判断，未经验证。**

---

## 一、实测数据

### 1.1 HTTP 协议版本（curl 不支持 h2，改用 TLS ALPN 探测）

| 服务 | ALPN 协商结果 |
|------|--------------|
| `webrd01.is.autonavi.com`（高德瓦片） | **h2** |
| `www.amap.com` | **h2** |
| `server.arcgisonline.com`（Esri） | http/1.1 |
| `tile.openstreetmap.de` | http/1.1 |
| 本项目本地服务器 | **HTTP/1.1** |

〔实测〕第一次用 `curl -w %{http_version}` 测全是 HTTP/1.1，后来查 `curl --version`
发现**本机 curl 根本没编译 HTTP/2**（Features 里没有 HTTP2），所以那次测试无效。
改用 Python 的 `ssl` 手工做 ALPN 协商才拿到真实结果。

> 教训：工具不支持某个特性时，测试会给出"看起来正常"的假结论。

### 1.2 服务器并发扩展性（240 个 Range 请求，4 段字节）

```
并发  1: 总   202ms  吞吐 1189 req/s  中位 0.8ms  p95  1.0ms
并发  3: 总   121ms  吞吐 1985 req/s  中位 1.4ms  p95  2.1ms
并发  6: 总   120ms  吞吐 1994 req/s  中位 2.7ms  p95  4.1ms
并发 12: 总   120ms  吞吐 2000 req/s  中位 5.1ms  p95  8.9ms
并发 24: 总   142ms  吞吐 1688 req/s  中位 8.6ms  p95 33.0ms
并发 48: 总   166ms  吞吐 1443 req/s  中位 8.3ms  p95 77.0ms
```

〔实测〕**吞吐在 3 条连接就接近峰值，24 条以上反而下降。**

**结论：浏览器对 HTTP/1.1 的"每域名 6 条连接"限制，对本项目不是瓶颈。**
所以"换 HTTP/2 就能变快"这条路**对我们无效**——服务器本身跑满了。

（高德用 h2 对他们有意义，因为他们的服务器能扛住高并发；我们的 Python
`http.server` 扛不住，h2 只会让更多请求同时挤进来然后一起变慢。）

### 1.3 服务端单次请求耗时

```
头部读取      17.2ms（首次，含连接建立）
根目录        1.1ms
叶子目录      1.3ms
单张瓦片      0.9ms（纯 HTTP Range）
```

〔实测〕**服务器极快。** 瓶颈不在服务端。

---

## 二、推断：高德流畅的真正原因

### 2.1 矢量渲染 + WebGL〔推断〕

高德网页版的地图是 **WebGL 矢量渲染**（他们页面里出现了 `webgl` 字样；
其栅格接口 `webrd*` / `webst*` 是给静态图片用的旧 API，不是动态地图用的）。

矢量渲染带来三个根本差异：

| | 矢量（高德） | 栅格（本项目） |
|---|---|---|
| **缩放** | GPU 矩阵变换，**不取数据** | **每一级都要重新取一整套图片** |
| **平移** | 用缓存几何重新绘制 | 视口外的新瓦片要重新取 |
| **单屏数据量** | 一张矢量瓦片 ≈ 十几张栅格瓦片 | 40+ 张独立图片 |

**缩放不需要联网，这是"流畅"的核心。** 高德缩放时只是把矢量数据重新光栅化，
数据早就本地有了，所以是"跟手"的。

### 2.2 我们的架构决定了做不到

我们的 UOM 数据是**二值栅格**（PNG8，alpha 只有 0/255）。用栅格瓦片展示，
就意味着**每一级缩放都要向服务器取一整套新图片**——这是栅格方案的固有代价，
不是调参能解决的。

**唯一的例外**：`maxNativeZoom` 机制下，放大超过 13 级时 Leaflet 直接拉伸
13 级瓦片，**不取新数据**，那部分是瞬时的。可以据此自测：放大到 14 级以上
应该明显比缩小更跟手。

---

## 三、可行的优化方向与代价

| 方案 | 效果 | 代价 |
|------|------|------|
| **A. 服务端合成 512px 瓦片** | 一屏请求 40+ → 12 张，实测装满时间 1250ms → **65ms** | 服务端要合成 PNG（已加目录缓存+二分查找，冷启动约 25ms/张）；失去单区域 popup 之类客户端能力。**曾经实现过（v16），按要求回退了** |
| **B. 空闲预取相邻层级** | 预取完成后缩放即时 | 抢带宽；只在用户停手 600ms 后启动（v15 已实现） |
| **C. 矢量化 + WebGL** | **根治**，缩放平移都不取数据 | 工作量大：二值栅格矢量化、几何简化、按需分块加载、写 WebGL 渲染器。且低缩放下的锯齿和平滑度需要额外处理 |
| ~~D. 换 HTTP/2~~ | ~~解除 6 连接限制~~ | **实测无效**：服务器在 3 连接就满负荷了 |

### 建议顺序

1. **先做 A**（收益最大、改动可控、已验证过 19 倍提升）
2. B 作为补充（成本低）
3. C 只有在"必须做到和高德一样的跟手"时才值得投入

---

## 四、排查性能问题的教训（本项目踩过的）

| 错误做法 | 后果 | 正确做法 |
|---------|------|---------|
| 靠 `longtask` 的总时长猜是谁 | 连续改错两版（闪烁、更卡） | 按组件分别计时归因（PROF） |
| 凭印象认为 DJI 绘制是瓶颈 | 实测峰值只有 4ms，方向完全错 | 先测再改 |
| 在库已有机制的地方叠加自己的实现 | ① 瓦片 opacity 过渡覆盖了库的 `transition:none` → 每次缩放闪一下 ② `TILE_OPTS` 里的 `tileSize` 覆盖了图层的 512 设置 → 适飞层完全不显示 | 改动前先确认库在这块做了什么 |
| 一次改多项，无法归因 | v13 更卡了但不知道是哪项造成 | 一次只改一项 |
